apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: wisecow-network-audit
  namespace: default
spec:
  severity: 5
  selector:
    matchLabels:
      app: wisecow
  network:
    matchProtocols:
    - protocol: icmp
  action: Audit
